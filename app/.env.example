# Copy to .env (not committed) and fill in values.
# For MVP spike we support token-based auth.
# Later we will implement Plex web auth and store tokens securely.

PLEX_BASE_URL=http://YOUR_PLEX_SERVER:32400
PLEX_TOKEN=YOUR_PLEX_TOKEN
